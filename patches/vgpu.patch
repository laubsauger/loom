diff --git a/dist/api-types.d.ts b/dist/api-types.d.ts
index e444670e232e0d03f6387c3a35e3f5959c668c71..a9f33aa51b8e4c558b736423c21d7705f0964b3e 100644
--- a/dist/api-types.d.ts
+++ b/dist/api-types.d.ts
@@ -5,7 +5,9 @@
  * can be imported — and tree-shaken — without pulling the object that owns every factory.
  */
 import type { VGPUError } from "./errors.ts";
+import type { Frame } from "./frame.ts";
 import type { Target } from "./target.ts";
+import type { TimerSpan } from "./timer.ts";
 export interface ComputeOptions {
     readonly label?: string;
     readonly set?: Record<string, unknown>;
@@ -21,10 +23,22 @@ export interface DispatchOptions {
         readonly offset?: number;
     };
 }
+/**
+ * shaderloom patch (T1247): where a dispatch's GPU timestamp pair is billed.
+ *
+ * A `Compute` submits its own command buffer, so it cannot take a `timer` the way
+ * `FramePassOptions` does. The span is attached to the OPEN FRAME handed here, which is what
+ * puts the dispatch inside that frame's resolve — and therefore inside the per-pass column and
+ * the frame extent that vgpu reports for the submit (patch theme 4).
+ */
+export interface DispatchTiming {
+    readonly timer: TimerSpan;
+    readonly frame: Frame;
+}
 export interface Compute {
     set(values: Record<string, unknown>): this;
-    dispatch(x: number, y?: number, z?: number): void;
-    dispatch(opts: DispatchOptions): void;
+    dispatch(x: number, y?: number, z?: number, timing?: DispatchTiming): void;
+    dispatch(opts: DispatchOptions & Partial<DispatchTiming>): void;
 }
 export type StorageAccess = "read" | "read-write";
 export interface StorageOptions {
diff --git a/dist/compute.d.ts b/dist/compute.d.ts
index 287bdf781aa076099649632b1d6effc1fdaba2d7..a971889ae90d3d271e5c84b4a4ce621168de90bd 100644
--- a/dist/compute.d.ts
+++ b/dist/compute.d.ts
@@ -3,7 +3,7 @@ import type { ShaderSource } from "@vgpu/wgsl";
 import { type Reflection } from "@vgpu/wgsl/reflect-source";
 import { type BindGroupCache } from "./bind-cache.ts";
 import { type SetBag, type SetCore } from "./set-core.ts";
-import type { Compute, ComputeOptions, DispatchOptions } from "./api-types.ts";
+import type { Compute, ComputeOptions, DispatchOptions, DispatchTiming } from "./api-types.ts";
 import type { Gpu } from "./kernel.ts";
 /**
  * Compute pipeline for this gpu, ready to `set()` bindings and `dispatch()`.
@@ -35,7 +35,7 @@ export declare class ComputePipeline implements Compute {
     readonly pipeline: GPUComputePipeline;
     constructor(device: Device, source: string, opts?: ComputeOptions, cache?: BindGroupCache);
     set(values: SetBag): this;
-    dispatch(x: number, y?: number, z?: number): void;
-    dispatch(opts: DispatchOptions): void;
+    dispatch(x: number, y?: number, z?: number, timing?: DispatchTiming): void;
+    dispatch(opts: DispatchOptions & Partial<DispatchTiming>): void;
 }
 //# sourceMappingURL=compute.d.ts.map
\ No newline at end of file
diff --git a/dist/compute.js b/dist/compute.js
index fa1d930d97886ed6168d1ce9598f810e9ce61e15..dbfa741a882f8dce3a59aaf5f62607e5ca72cc87 100644
--- a/dist/compute.js
+++ b/dist/compute.js
@@ -71,26 +71,62 @@ export class ComputePipeline {
         if (opts.set)
             this.set(opts.set);
     }
+    /**
+     * shaderloom patch (T1180): drop this compute's cached bind groups; see
+     * InternalDraw.evictBindGroups. A compute shares the gpu's one bind cache with every draw
+     * while `nextComputeId` is a counter separate from `nextDrawId`, so one id can name both —
+     * the worst case is still a rebuilt bind group on the other one's next encode, never a
+     * wrong one. Its own pipeline and shader module are per-instance and are left to the GC.
+     */
+    evictBindGroups() {
+        this.cache.clearDraw(this.id);
+    }
     set(values) {
         assertDeviceUsable(this.device, `${this.label}.set`);
         this.setCore.set(values);
         return this;
     }
-    dispatch(x, y, z) {
+    dispatch(x, y, z, timing) {
         assertDeviceUsable(this.device, `${this.label}.dispatch`);
-        const indirect = typeof x === "object" && x !== null ? this.#resolveIndirectDispatch(x, y, z) : undefined;
+        const opts = typeof x === "object" && x !== null ? x : undefined;
+        const indirect = opts ? this.#resolveIndirectDispatch(x, y, z) : undefined;
         this.#preflightAliasing();
-        const encoder = this.device.gpu.createCommandEncoder({ label: `${this.label}.encoder` });
-        const pass = encoder.beginComputePass({ label: `${this.label}.pass` });
-        pass.setPipeline(this.pipeline);
-        for (const binding of this.setCore.bindGroups())
-            pass.setBindGroup(binding.group, binding.bindGroup, binding.offsets);
-        if (indirect)
-            pass.dispatchWorkgroupsIndirect(indirect.buffer, indirect.offset);
-        else
-            pass.dispatchWorkgroups(x, y ?? 1, z ?? 1);
-        pass.end();
-        this.device.gpu.queue.submit([encoder.finish()]);
+        /**
+         * shaderloom patch (T1247): optional GPU timestamp pair around this dispatch. The span
+         * belongs to the open frame that is handed with it, never to this command buffer — see
+         * Frame.attachExternalSpan. Attached AFTER every throwing precondition above, so a
+         * dispatch that never encodes cannot leave a reserved query pair the frame would resolve
+         * out of an unwritten query set.
+         */
+        const timed = opts?.timer !== undefined ? opts : timing;
+        const attachment = timed?.timer !== undefined && timed.frame !== undefined
+            ? timed.frame.attachExternalSpan(timed.timer)
+            : undefined;
+        const timestampWrites = attachment?.timestampWrites;
+        // shaderloom patch (T1295): the pair above is now reserved in the frame. If anything below
+        // throws, no timestamp was written into it, so the span is retracted rather than resolved
+        // out of an unwritten query set as a duration for a dispatch that never ran.
+        let submitted = false;
+        try {
+            const encoder = this.device.gpu.createCommandEncoder({ label: `${this.label}.encoder` });
+            const pass = encoder.beginComputePass(timestampWrites
+                ? { label: `${this.label}.pass`, timestampWrites }
+                : { label: `${this.label}.pass` });
+            pass.setPipeline(this.pipeline);
+            for (const binding of this.setCore.bindGroups())
+                pass.setBindGroup(binding.group, binding.bindGroup, binding.offsets);
+            if (indirect)
+                pass.dispatchWorkgroupsIndirect(indirect.buffer, indirect.offset);
+            else
+                pass.dispatchWorkgroups(x, y ?? 1, z ?? 1);
+            pass.end();
+            this.device.gpu.queue.submit([encoder.finish()]);
+            submitted = true;
+        }
+        finally {
+            if (!submitted)
+                attachment?.retract();
+        }
     }
     /** The GPU reads the workgroup counts from the buffer, so explicit counts alongside indirect are dead options and throw. */
     #resolveIndirectDispatch(opts, y, z) {
diff --git a/dist/draw.d.ts b/dist/draw.d.ts
index 3a363e3a6b8df312ab2e8b78f6d76d472c09fb94..70b440489134c1131403f3b69f3b8a1833c5ebc0 100644
--- a/dist/draw.d.ts
+++ b/dist/draw.d.ts
@@ -4,6 +4,8 @@ import { type Reflection } from "@vgpu/wgsl/reflect-source";
 import { type BindGroupCache } from "./bind-cache.ts";
 import { type ClaimedGroupValidationResult, type ValidationErrorSink } from "./claim-validation.ts";
 import { type BindingIdentityChange, type BindingState, type SetBag } from "./set-core.ts";
+import type { Frame } from "./frame.ts";
+import type { TimerSpan } from "./timer.ts";
 import type { CompileTarget, Target, TargetSignature } from "./target.ts";
 import { type PipelineLayoutCache, type PipelineStore, type ShaderModuleCache } from "./pipeline-store.ts";
 import type { StorageBuffer } from "./api-types.ts";
@@ -135,6 +137,13 @@ export interface DrawCallOptions {
         readonly buffer: StorageBuffer;
         readonly offset?: number;
     };
+    /**
+     * shaderloom patch (T1247): time this self-submitted draw against an OPEN FRAME, the way
+     * `FramePassOptions.timer` times a pass of that frame. Both members or neither; see
+     * `DispatchTiming` in api-types.ts for why the frame is part of the request.
+     */
+    readonly timer?: TimerSpan;
+    readonly frame?: Frame;
 }
 export interface DrawLayoutOptions {
     readonly dynamicOffsets?: boolean;
diff --git a/dist/draw.js b/dist/draw.js
index d6011f8aba886f4a9f5f4e15b7da07fe889ad006..eac7c0b03a71525a8bb6621a4ac74241d25893d2 100644
--- a/dist/draw.js
+++ b/dist/draw.js
@@ -102,6 +102,35 @@ export class InternalDraw {
     writesDepth() { return drawWritesDepth(this); }
     /** @internal Frame drawable protocol; see {@link drawStencilWritingOps}. */
     stencilWritingOps() { return drawStencilWritingOps(this); }
+    /**
+     * shaderloom patch (T1180): drop this draw's cached bind groups. It calls the cache's own
+     * `clearDraw`, which upstream defines and never calls; nothing else can reach it, because
+     * the cache is a private kernel service and `drawId` is a closure-local counter.
+     *
+     * Two ways entries become unreachable, and neither has a public exit today:
+     *
+     * 1. A DISCARDED drawable. The key is `drawId:group:identities` and every Draw/Effect
+     *    mints a fresh `nextDrawId`, so a program rebuild's replaced Effects leave their
+     *    entries behind for the life of the device — Draw/Effect/Compute expose no destroy at
+     *    all. `subscribeEviction` reclaims an entry only when a resource it names is
+     *    destroyed, so entries over surviving resources (a shader edit rebinding the same
+     *    target) survive with it.
+     *
+     * 2. A LIVE drawable whose bindings ROTATE. The eviction subscription is per binding
+     *    SLOT, not per cache entry: `set()`ing a slot to a new resource unsubscribes the
+     *    previous one, so an entry naming the old resource outlives that resource's destroy
+     *    with nothing listening. A blit that re-points one texture slot at each of N targets
+     *    keeps N entries and is subscribed to one.
+     *
+     * Only this draw's own entries go — the drawId prefix is unique per instance — and any
+     * entry still wanted is rebuilt on the next encode (`setCore.bindGroups()` calls
+     * `getOrCreate` every time), so an over-eager evict costs a rebuild, never a wrong bind
+     * group. Pipelines, shader modules and layouts are shared by key and are untouched.
+     */
+    evictBindGroups() {
+        const state = drawState(this);
+        state.cache.clearDraw(state.id);
+    }
     set(values) {
         const state = drawState(this);
         assertDeviceUsable(state.device, `${this.label}.set`);
@@ -151,71 +180,94 @@ export class InternalDraw {
             throw targetRequiredError(`${this.label}.draw`);
         assertSurfaceTargetInFrame(target, `${this.label}.draw`);
         const encoder = state.device.gpu.createCommandEncoder();
-        const pass = encoder.beginRenderPass(target.renderPassDescriptor());
-        const validations = [];
+        /**
+         * shaderloom patch (T1247): optional GPU timestamp pair around a self-submitted draw.
+         * `Draw.draw()` is the only way to issue a GPU-driven (indirect) draw and, like
+         * Compute.dispatch, it builds and submits its OWN command buffer — so it never reaches
+         * Frame.pass and never received a pair. The span is billed to the open frame handed with
+         * it (Frame.attachExternalSpan), whose resolve runs after this submit.
+         */
+        const attachment = opts.timer !== undefined && opts.frame !== undefined
+            ? opts.frame.attachExternalSpan(opts.timer)
+            : undefined;
+        const timestampWrites = attachment?.timestampWrites;
+        // shaderloom patch (T1295): every exit below that does not reach queue.submit — a throw, or the
+        // finish/submit validation early returns — wrote no timestamps into the reserved pair, so the
+        // span is retracted rather than resolved as a phantom duration.
+        let submitted = false;
         try {
-            this.encode(pass, target, opts, (result) => validations.push(result));
-        }
-        catch (error) {
-            discardClaimedGroupValidationResults(validations);
-            discardClaimedGroupValidationScopes(state.device);
+            const descriptor = target.renderPassDescriptor();
+            const pass = encoder.beginRenderPass(timestampWrites ? { ...descriptor, timestampWrites } : descriptor);
+            const validations = [];
             try {
-                pass.end();
+                this.encode(pass, target, opts, (result) => validations.push(result));
             }
-            catch { /* ignore cleanup failure after encode failure */ }
-            throw error;
-        }
-        endRenderPassWithClaimValidation(state.device, pass, validations, validations[0]?.context);
-        let commandBuffer;
-        const finishContext = validations[0]?.context;
-        if (finishContext)
-            pushClaimedGroupValidationScope(state.device, finishContext);
-        try {
-            commandBuffer = encoder.finish();
-        }
-        catch (error) {
-            const result = finishContext ? popLastClaimedGroupValidationScope(state.device) : undefined;
-            discardClaimedGroupValidationResults(validations);
-            if (result)
-                discardClaimedGroupValidationResults([result]);
-            const context = result?.context ?? finishContext;
-            if (context) {
-                void reportDrawValidationError(state, context.label, context.group, error);
-                return;
+            catch (error) {
+                discardClaimedGroupValidationResults(validations);
+                discardClaimedGroupValidationScopes(state.device);
+                try {
+                    pass.end();
+                }
+                catch { /* ignore cleanup failure after encode failure */ }
+                throw error;
             }
-            throw error;
-        }
-        if (finishContext) {
-            const result = popLastClaimedGroupValidationScope(state.device);
-            if (result)
-                validations[0] = validations[0] ? preferClaimedGroupValidationResult(result, validations[0]) : result;
-        }
-        const submitContext = validations[0]?.context;
-        if (submitContext)
-            pushClaimedGroupValidationScope(state.device, submitContext);
-        try {
-            state.device.gpu.queue.submit([commandBuffer]);
-        }
-        catch (error) {
-            const result = submitContext ? popLastClaimedGroupValidationScope(state.device) : undefined;
-            discardClaimedGroupValidationResults(validations);
-            if (result)
-                discardClaimedGroupValidationResults([result]);
-            const context = result?.context ?? submitContext;
-            if (context) {
-                void reportDrawValidationError(state, context.label, context.group, error);
-                return;
+            endRenderPassWithClaimValidation(state.device, pass, validations, validations[0]?.context);
+            let commandBuffer;
+            const finishContext = validations[0]?.context;
+            if (finishContext)
+                pushClaimedGroupValidationScope(state.device, finishContext);
+            try {
+                commandBuffer = encoder.finish();
+            }
+            catch (error) {
+                const result = finishContext ? popLastClaimedGroupValidationScope(state.device) : undefined;
+                discardClaimedGroupValidationResults(validations);
+                if (result)
+                    discardClaimedGroupValidationResults([result]);
+                const context = result?.context ?? finishContext;
+                if (context) {
+                    void reportDrawValidationError(state, context.label, context.group, error);
+                    return;
+                }
+                throw error;
+            }
+            if (finishContext) {
+                const result = popLastClaimedGroupValidationScope(state.device);
+                if (result)
+                    validations[0] = validations[0] ? preferClaimedGroupValidationResult(result, validations[0]) : result;
+            }
+            const submitContext = validations[0]?.context;
+            if (submitContext)
+                pushClaimedGroupValidationScope(state.device, submitContext);
+            try {
+                state.device.gpu.queue.submit([commandBuffer]);
+                submitted = true;
+            }
+            catch (error) {
+                const result = submitContext ? popLastClaimedGroupValidationScope(state.device) : undefined;
+                discardClaimedGroupValidationResults(validations);
+                if (result)
+                    discardClaimedGroupValidationResults([result]);
+                const context = result?.context ?? submitContext;
+                if (context) {
+                    void reportDrawValidationError(state, context.label, context.group, error);
+                    return;
+                }
+                throw error;
+            }
+            if (submitContext) {
+                const result = popLastClaimedGroupValidationScope(state.device);
+                if (result)
+                    validations[0] = validations[0] ? preferClaimedGroupValidationResult(result, validations[0]) : result;
+            }
+            if (validations.length) {
+                const done = claimedGroupValidationDone(state.device, validations, { errorSink: state.errorSink });
+                state.trackSettled?.(done);
             }
-            throw error;
-        }
-        if (submitContext) {
-            const result = popLastClaimedGroupValidationScope(state.device);
-            if (result)
-                validations[0] = validations[0] ? preferClaimedGroupValidationResult(result, validations[0]) : result;
         }
-        if (validations.length) {
-            const done = claimedGroupValidationDone(state.device, validations, { errorSink: state.errorSink });
-            state.trackSettled?.(done);
+        finally {
+            if (!submitted)
+                attachment?.retract();
         }
     }
     encode(pass, target, opts = {}, claimValidation) {
diff --git a/dist/effect.js b/dist/effect.js
index 079c6686ce2c87a580f217b12c9ea978f0ed3005..fb684c09ba66ed7763f8f2123a5f6ec635bda0ea 100644
--- a/dist/effect.js
+++ b/dist/effect.js
@@ -31,6 +31,8 @@ export class InternalEffect {
         effectImpls.set(this, impl);
     }
     set(values) { effectImpl(this).set(values); return this; }
+    /** shaderloom patch (T1180): forwards to the underlying draw; see InternalDraw.evictBindGroups. */
+    evictBindGroups() { effectImpl(this).evictBindGroups(); }
     draw(target = {}) { effectImpl(this).draw(isTarget(target) ? { target } : target); }
     compile(target) { return effectImpl(this).compile(target).then(() => this); }
     compileSync(target) { effectImpl(this).compileSync(target); return this; }
diff --git a/dist/frame.d.ts b/dist/frame.d.ts
index 195a58c3a3a8f8c01f53307ec31e5449d1608748..efa932e214e1a318e1c8c245e62b65192634e656 100644
--- a/dist/frame.d.ts
+++ b/dist/frame.d.ts
@@ -82,6 +82,16 @@ export declare class Frame {
     constructor(device: Device, defaultTarget?: Target | undefined, errorSink?: ValidationErrorSink | undefined, trackSettled?: ((promise: Promise<unknown>) => void) | undefined, releaseLifecycle?: (() => void) | undefined);
     pass(target: Target, body: Effect | Draw | ((pass: FramePass) => void)): void;
     pass(options: FramePassOptions, body: Effect | Draw | ((pass: FramePass) => void)): void;
+    /**
+     * shaderloom patch (T1247): times GPU work submitted outside this frame's encoder (a vgpu
+     * `Compute` submits its own command buffer) into THIS frame's span set, so the per-pass column
+     * and the frame extent cover compute dispatches. See the implementation note in frame.js.
+     */
+    attachExternalSpan(span: TimerSpan): {
+        readonly timestampWrites?: GPUComputePassTimestampWrites;
+        /** shaderloom patch (T1295): undo, for work that attached and then never reached the queue. */
+        readonly retract: () => void;
+    };
     submit(): void;
     /**
      * Discards the frame without submitting it: the command encoder is dropped (nothing this frame
diff --git a/dist/frame.js b/dist/frame.js
index 0cda97810db3bb9bec195d8399cd768baf9b8c2c..12c6848abe2c81541cd7da22c7180e74d29ebc29 100644
--- a/dist/frame.js
+++ b/dist/frame.js
@@ -119,8 +119,8 @@ export class Frame {
             throw surfaceNotInFrameError("Frame.pass");
         const clear = targetOnly ? undefined : target.clear;
         const preserve = clear === false;
-        if (preserve && resolvedTarget.sampleCount === 4)
-            throw passPreserveMsaaError();
+        // shaderloom patch (T939): preserve into MSAA is supported now — the msaa
+        // attachment stores every pass and loads on preserve (see colorAttachment).
         const clearDepth = targetOnly ? undefined : target.clearDepth;
         if (clearDepth !== undefined) {
             if (typeof clearDepth !== "number" || !(clearDepth >= 0 && clearDepth <= 1))
@@ -221,6 +221,36 @@ export class Frame {
         }
         endRenderPassWithClaimValidation(this.device, encoder, this.#validations);
     }
+    /**
+     * shaderloom patch (T1247): time GPU work that is NOT encoded into this frame's encoder.
+     *
+     * A vgpu `Compute` builds and submits its own command buffer (compute.js `dispatch`), so its
+     * `timestampWrites` cannot travel through `Frame.pass` — and without them a compute-heavy
+     * document's per-pass column is render-only and the frame EXTENT (patch theme 4) stops at the
+     * last render pass. Both under-report by exactly the compute work.
+     *
+     * Attaching here puts the span in THIS frame's span set through the same protocol a pass uses,
+     * so the frame's single `resolveQuerySet` covers the compute's query indices too. That is
+     * correct by queue order, not by luck: the compute's command buffer is submitted while this
+     * frame is still open, so its timestamp writes execute before this frame's resolve does.
+     *
+     * Returns the attachment result; `timestampWrites` is undefined when the span was dropped for
+     * this frame (query set full), exactly as in `Frame.pass`.
+     */
+    attachExternalSpan(span) {
+        if (this.#canceled)
+            throw frameCanceledError("Frame.attachExternalSpan");
+        if (this.#submitted)
+            throw frameAlreadySubmittedError("Frame.attachExternalSpan");
+        assertDeviceUsable(this.device, "Frame.attachExternalSpan");
+        const result = this.#attach(span, undefined, [], timerAttachmentInvalidError);
+        // shaderloom patch (T1295): the caller's undo, for work that attached a span and then never
+        // reached the queue. See InternalTimer.retractSpan.
+        return {
+            timestampWrites: result.timestampWrites,
+            retract: () => { result.owner.retractSpan?.(span, this); },
+        };
+    }
     submit() {
         // Closed either way: a re-submit has nothing left to flush, and a canceled frame dropped its
         // encoder. Both are silent no-ops so `frame(gpu, cb)`'s implicit submit-on-return never masks
diff --git a/dist/index.d.ts b/dist/index.d.ts
index f11060a2a7f9b234465d04371e8826941e6b8d90..b301ef54962d1ed25106e5b321f33dcde9b59af0 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,6 +1,6 @@
 import { createGpu, type InitOptions } from "./init.ts";
 export { initFromDevice } from "./init-from-device.ts";
-export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
+export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, DispatchTiming, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerFrameDrop, TimerFrameExtent, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
 export type { BlendComponentOptions, BlendOptions, BlendPreset, DepthOptions, Draw, DrawOptions, DrawCallOptions, DrawLayoutOptions, GeometryLike, StencilFaceOptions, StencilOptions } from "./draw.ts";
 export { Geometry } from "./scene/geometry-descriptor.ts";
 export type { GeometryAttributeOverride, GeometryAttributes, GeometryBuffer, GeometryBufferOptions, GeometryData, GeometryOptions, GeometrySlice, GeometrySliceOptions } from "./scene/geometry-descriptor.ts";
diff --git a/dist/init.d.ts b/dist/init.d.ts
index d4cb959be6bc7d6e9c8fbad4d42cd90058c1ade1..ef89e3a22e6e138a7106b86e75555908eeb1efb5 100644
--- a/dist/init.d.ts
+++ b/dist/init.d.ts
@@ -1,9 +1,9 @@
 export { createCoreGpu as createGpu, type Gpu } from "./kernel.ts";
 export { initFromDevice } from "./init-from-device.ts";
 export type { AdapterFactory, InitOptions } from "./kernel.ts";
-export type { Compute, ComputeOptions, DispatchOptions, GpuErrorListener, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions } from "./api-types.ts";
+export type { Compute, ComputeOptions, DispatchOptions, DispatchTiming, GpuErrorListener, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions } from "./api-types.ts";
 export type { ClearColor } from "./target-utils.ts";
-export type { Timer, TimerSpan } from "./timer.ts";
+export type { Timer, TimerFrameDrop, TimerFrameExtent, TimerSpan } from "./timer.ts";
 export type { Visibility, VisibilityOptions, VisibilityQuery } from "./visibility.ts";
 export type { Bundle, BundleOptions, BundleRecorder } from "./bundle.ts";
 export type { Surface, SurfaceOptions, SurfaceResizeEvent } from "./surface.ts";
diff --git a/dist/mock.d.ts b/dist/mock.d.ts
index 1a10c694a982bde7440229d528c1e7c973a9a389..2b33e90ca6fe3a6ccbfc5c83cfec9a5ae254dbbd 100644
--- a/dist/mock.d.ts
+++ b/dist/mock.d.ts
@@ -3,7 +3,7 @@ export { initFromDevice } from "./init-from-device.ts";
 export { createMockAdapter } from "@vgpu/adapter-mock";
 export type { CreateMockAdapterOptions } from "@vgpu/adapter-mock";
 export { getMockGPUDeviceInstrumentation } from "@vgpu/core";
-export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
+export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, DispatchTiming, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerFrameExtent, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
 export type { BlendComponentOptions, BlendOptions, BlendPreset, DepthOptions, Draw, DrawOptions, DrawCallOptions, DrawLayoutOptions, GeometryLike, StencilFaceOptions, StencilOptions } from "./draw.ts";
 export { Geometry } from "./scene/geometry-descriptor.ts";
 export type { GeometryAttributeOverride, GeometryAttributes, GeometryBuffer, GeometryBufferOptions, GeometryData, GeometryOptions, GeometrySlice, GeometrySliceOptions } from "./scene/geometry-descriptor.ts";
diff --git a/dist/node.d.ts b/dist/node.d.ts
index c7b92ed4fba8ab1d0641d585d80c85e49bb6a6f9..36015c3285f00e873fccb742f5fab6a858bd4bb7 100644
--- a/dist/node.d.ts
+++ b/dist/node.d.ts
@@ -2,7 +2,7 @@ import { type NodeAdapterInfo, type NodeAdapterMode } from "@vgpu/adapter-node";
 import type { VGPUAdapter } from "@vgpu/core";
 import { type Gpu, type InitOptions } from "./init.ts";
 export { createNodeAdapter } from "@vgpu/adapter-node";
-export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
+export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, DispatchTiming, ClearColor, GpuErrorListener, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerFrameExtent, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
 export type { BlendComponentOptions, BlendOptions, BlendPreset, DepthOptions, Draw, DrawOptions, DrawCallOptions, DrawLayoutOptions, GeometryLike, StencilFaceOptions, StencilOptions } from "./draw.ts";
 export { Geometry } from "./scene/geometry-descriptor.ts";
 export type { GeometryAttributeOverride, GeometryAttributes, GeometryBuffer, GeometryBufferOptions, GeometryData, GeometryOptions, GeometrySlice, GeometrySliceOptions } from "./scene/geometry-descriptor.ts";
diff --git a/dist/set-resources.js b/dist/set-resources.js
index 43d3530106957dcbd764af7bd07f91b8d8b14ef3..ccc4d62b4cd3708633a7a0473b3d9c8d89a622cd 100644
--- a/dist/set-resources.js
+++ b/dist/set-resources.js
@@ -47,8 +47,15 @@ function normalizeBufferResource(binding, value, context) {
         assertBufferUsable(value.buffer, `${context.sourceHint}.set`);
         return { resource: { buffer: value.gpu, offset: 0, size: value.size }, identity: value.buffer.resourceIdentity, unsubscribe: (cb) => value.buffer.onDestroy(cb) };
     }
-    if (isGPUBufferBinding(value))
-        return { resource: value, identity: syntheticIdentity(value.buffer) };
+    if (isGPUBufferBinding(value)) {
+        // shaderloom patch (T1076): a REGION binding may name its own identity. Without
+        // this, every region of one buffer shares that buffer's synthetic identity, so the
+        // bind-group cache cannot tell two attributes apart and #preflightAliasing reports
+        // two NON-OVERLAPPING writable regions as an alias. Point storage is packed —
+        // several attributes per buffer — and both consequences are wrong.
+        const region = { buffer: value.buffer, offset: value.offset, size: value.size };
+        return { resource: region, identity: value.resourceIdentity ?? syntheticIdentity(value.buffer) };
+    }
     if (isRawGPUBuffer(value))
         return { resource: { buffer: value }, identity: syntheticIdentity(value) };
     throw incompatibleResourceError(binding, "buffer", `Pass a compatible Buffer/Uniform: ${binding.name}.set({ ${binding.name}: gpu.device.createBuffer(...) }).`);
@@ -113,7 +120,13 @@ function asTarget(value) {
 }
 function hasAnyResourceShape(value) {
     const record = value;
-    return "gpu" in record || "bindGroup" in record || "createView" in record || "resourceIdentity" in record;
+    // shaderloom patch (T1076): a raw GPUBufferBinding ({ buffer, offset, size }) is a
+    // resource, not a JS uniform value. Without this it falls to isPlainValue() and the
+    // set() routes it at the uniform path, throwing VGPU-RING1-UNSUPPORTED — even though
+    // normalizeBufferResource() below already handles the shape (isGPUBufferBinding).
+    // Binding one region of a packed buffer is how point attributes reach shaders that
+    // still declare array<vec3f>.
+    return "gpu" in record || "bindGroup" in record || "createView" in record || "resourceIdentity" in record || isGPUBufferBinding(record);
 }
 function syntheticIdentity(value) {
     if (typeof value !== "object" || value === null)
diff --git a/dist/target-offscreen.js b/dist/target-offscreen.js
index 3b4221148054e92c95a08f089a637249297b8d51..bbe26d4d064ec9c644c47c4a79d79a6ad64c7c0d 100644
--- a/dist/target-offscreen.js
+++ b/dist/target-offscreen.js
@@ -87,6 +87,8 @@ export class OffscreenTarget {
             size: this.#currentSize,
             format: spec.format,
             usage: ["render_attachment", "texture_binding", "copy_src"],
+            // shaderloom patch (T1307): same allocation, alternate sampling interpretation.
+            viewFormats: this.options.viewFormats,
             sampleCount: 1,
             label: this.options.label ? `${this.options.label}.color${index}.resolve` : undefined,
         }));
diff --git a/dist/target-utils.js b/dist/target-utils.js
index ec39e15a23d7dfdf7172e74d7c7305b34670a3e6..f9f04834e0554f53331cdb50872d04f6ef5cf94c 100644
--- a/dist/target-utils.js
+++ b/dist/target-utils.js
@@ -53,11 +53,15 @@ function validateMsaaFormat(format, caps) {
     throw unsupportedError("target", "Dawn compatibility mode does not support rgba16float+msaa.", "Use rgba8unorm for MSAA here, or disable msaa.");
 }
 export function colorAttachment(resolved, msaa, clear, preserve) {
+    // shaderloom patch (T939): multi-pass MSAA. The multisampled texture persists on the
+    // target, so preserve is legal WebGPU: load it, store it, and resolve every pass so
+    // the resolved color stays current for mid-chain readers. Non-preserve passes keep
+    // the cheaper discard.
     const attachment = {
         view: (msaa ?? resolved).createView(),
         resolveTarget: msaa ? resolved.createView() : undefined,
         loadOp: preserve ? "load" : "clear",
-        storeOp: msaa ? "discard" : "store",
+        storeOp: "store",
     };
     if (!preserve)
         attachment.clearValue = colorValue(clear);
@@ -74,13 +78,15 @@ export function depthAttachment(depth, preserve, clearDepth, clearStencil, readO
             attachment.stencilReadOnly = true;
         return attachment;
     }
-    const attachment = { view: depth.createView(), depthLoadOp: preserve ? "load" : "clear", depthStoreOp: depth.sampleCount > 1 ? "discard" : "store" };
+    // shaderloom patch (T939): multisampled depth STORES too — a later preserve pass
+    // depth-tests against it, and a discarded depth would load garbage.
+    const attachment = { view: depth.createView(), depthLoadOp: preserve ? "load" : "clear", depthStoreOp: "store" };
     if (!preserve)
         attachment.depthClearValue = clearDepth ?? 1;
     // WebGPU requires stencilLoadOp/stencilStoreOp whenever the format has a stencil aspect and stencilReadOnly is not set.
     if (depth.format && hasStencilAspect(depth.format)) {
         attachment.stencilLoadOp = preserve ? "load" : "clear";
-        attachment.stencilStoreOp = depth.sampleCount > 1 ? "discard" : "store";
+        attachment.stencilStoreOp = "store";
         if (!preserve)
             attachment.stencilClearValue = clearStencil ?? 0;
     }
diff --git a/dist/target.d.ts b/dist/target.d.ts
index f642ba8b4a9ba90b3d3aff09484779766564f86a..fcaa44da00b451720e69d0347dabe6244cc933e1 100644
--- a/dist/target.d.ts
+++ b/dist/target.d.ts
@@ -2,6 +2,8 @@ import type { ClearColor } from "./target-utils.ts";
 import type { Texture, ResourceDestroyCallback, ResourceIdentity, UnsubscribeResourceDestroy } from "@vgpu/core";
 export interface TargetTextureOptions {
     readonly format?: GPUTextureFormat;
+    /** shaderloom T1307: compatible views for each resolved color attachment; not depth/MSAA storage. */
+    readonly viewFormats?: readonly GPUTextureFormat[];
     /**
      * Default clear color of this target, used by passes that clear without naming a color.
      * Defaults to `[0, 0, 0, 1]`; mutable at runtime through `target.clearColor`.
diff --git a/dist/timer.d.ts b/dist/timer.d.ts
index 4571da5a860793c7250ad8862c5341d700217765..04998a9ceec949475f1cb4005900f244d53e8bdc 100644
--- a/dist/timer.d.ts
+++ b/dist/timer.d.ts
@@ -10,12 +10,33 @@ import { type QueryHostOptions } from "./query-ring.ts";
 export interface TimerSpan {
     readonly name: string;
 }
+/**
+ * shaderloom patch (T1243): what one frame's spans say about the FRAME. `extentMs` is the earliest
+ * span begin to the latest span end of that frame's submit; `frame` is the identity of the vgpu
+ * frame the spans were attached to, so a caller can tie the result back to what it encoded.
+ */
+export interface TimerFrameExtent {
+    readonly extentMs: number;
+    readonly frame: object;
+}
+/**
+ * shaderloom patch (T1295): one frame whose attached spans will never produce an `onResults` call.
+ * `reason` is "staging-busy" (every staging slot was still waiting on an earlier readback) or
+ * "abandoned" (a failed pass discarded the timer for the frame, or the frame never reached the queue).
+ */
+export interface TimerFrameDrop {
+    readonly frame: object;
+    readonly reason: "staging-busy" | "abandoned";
+    readonly spans: number;
+}
 /** GPU pass timer created by `timer(gpu)`. Needs the "timestamp-query" device feature. */
 export interface Timer {
     /** Marks a pass for timing. Pass the result as FramePassOptions.timer. One name per frame. */
     span(name: string): TimerSpan;
-    /** Registers a callback invoked whenever a new set of results lands (typically 1-2 frames after submit). Times in milliseconds. Returns an unsubscribe function. */
-    onResults(cb: (spans: Readonly<Record<string, number>>) => void): () => void;
+    /** Registers a callback invoked whenever a new set of results lands (typically 1-2 frames after submit). Times in milliseconds. Returns an unsubscribe function. shaderloom patch (T1243): the frame's extent rides as a second argument. */
+    onResults(cb: (spans: Readonly<Record<string, number>>, frame: TimerFrameExtent) => void): () => void;
+    /** shaderloom patch (T1295): called once per frame whose spans will never produce a result. Returns an unsubscribe function. */
+    onDropped(cb: (drop: TimerFrameDrop) => void): () => void;
     /** Releases the timer's query set and buffers. Further use of the timer or its spans throws. */
     dispose(): void;
 }
@@ -54,8 +75,11 @@ export declare class InternalTimer {
     #private;
     constructor(device: Device, host?: QueryHostOptions);
     span(name: string): TimerSpan;
-    onResults(cb: (spans: Readonly<Record<string, number>>) => void): () => void;
+    onResults(cb: (spans: Readonly<Record<string, number>>, frame: TimerFrameExtent) => void): () => void;
+    onDropped(cb: (drop: TimerFrameDrop) => void): () => void;
     dispose(): void;
+    /** @internal shaderloom patch (T1295): Frame.attachExternalSpan's undo. */
+    retractSpan(span: TimerSpan, frame: unknown): void;
     /**
      * @internal Frame.pass hook: registers the span for the current frame and returns the pass
      * descriptor's timestampWrites, or undefined when the span is dropped for this frame because
diff --git a/dist/timer.js b/dist/timer.js
index 88f211164c7f4d19a965d45ef4cce5d75fe08086..0488902ba2a30085e8cafcc5b068ca03c3c16282 100644
--- a/dist/timer.js
+++ b/dist/timer.js
@@ -8,6 +8,14 @@ const INITIAL_SPAN_CAPACITY = 32;
 const MAX_QUERIES = 4096;
 const MAX_SPANS_PER_FRAME = MAX_QUERIES / 2;
 const MS_PER_NS = 1 / 1_000_000;
+/**
+ * shaderloom patch (T1295): the query ring starts with vgpu's three staging slots and doubles on
+ * demand, at a frame boundary, whenever a frame is dropped for want of a free one. Three is right for
+ * a frame loop (one frame per tick); a caller that submits several timed frames per tick — our export
+ * path splits a render into one frame per compute segment — needs one slot per frame in flight.
+ */
+const INITIAL_STAGING_DEPTH = 3;
+const MAX_STAGING_DEPTH = 32;
 class InternalTimerSpan {
     name;
     owner;
@@ -63,6 +71,12 @@ export class InternalTimer {
     #encodedSpans;
     #resultSeq = 0;
     #appliedSeq = -1;
+    /** shaderloom patch (T1295): the current ring's staging depth, and the depth a drop asked for. */
+    #depth = INITIAL_STAGING_DEPTH;
+    #depthDemand = INITIAL_STAGING_DEPTH;
+    #dropListeners = new Set();
+    /** The frame a drop was last reported for, so one frame is never reported twice. */
+    #droppedFrame;
     /**
      * One retain per open frame whose pass descriptors reference a ring's query set, keyed by frame
      * identity and remembering *which* ring it pinned (capacity growth swaps the ring mid-life).
@@ -100,11 +114,25 @@ export class InternalTimer {
         this.#listeners.add(cb);
         return () => { this.#listeners.delete(cb); };
     }
+    /**
+     * shaderloom patch (T1295): called once for every frame whose attached spans will NEVER produce
+     * an `onResults` call. Before this, such a frame was indistinguishable from one whose results had
+     * not landed yet: a missing figure that read as an absence rather than announcing itself.
+     * `reason` is "staging-busy" (every staging slot was still waiting on an earlier readback when the
+     * frame submitted) or "abandoned" (a failed pass discarded this timer for the frame, or the frame
+     * never reached the queue). `spans` is how many spans the frame had attached.
+     */
+    onDropped(cb) {
+        this.#assertUsable("Timer.onDropped");
+        this.#dropListeners.add(cb);
+        return () => { this.#dropListeners.delete(cb); };
+    }
     dispose() {
         if (this.#disposed)
             return;
         this.#disposed = true;
         this.#listeners.clear();
+        this.#dropListeners.clear();
         this.#spans.clear();
         this.#frameNames.clear();
         this.#frameSpans = [];
@@ -153,10 +181,33 @@ export class InternalTimer {
     /** @internal Frame.submit hook, before encoder.finish(): appends this frame's single resolve to the frame encoder. */
     finalizeFrame(frame, encoder) {
         this.#encodedSpans = undefined;
-        if (this.#disposed || frame !== this.#frame || this.#usedQueries === 0)
+        // shaderloom patch (T1295): a frame whose every span was retracted has nothing to read back.
+        if (this.#disposed || frame !== this.#frame || this.#usedQueries === 0 || this.#frameSpans.length === 0)
             return;
-        if (this.#ring.encodeResolve(encoder, this.#usedQueries))
+        if (this.#ring.encodeResolve(encoder, this.#usedQueries)) {
             this.#encodedSpans = [...this.#frameSpans];
+            return;
+        }
+        // shaderloom patch (T1295): every staging slot is still waiting on an earlier readback, so this
+        // frame's spans are lost. The ring drops rather than blocks, which is right for a frame loop —
+        // but the loss is now reported, and the next frame boundary opens a deeper ring.
+        if (this.#depth < MAX_STAGING_DEPTH)
+            this.#depthDemand = Math.min(MAX_STAGING_DEPTH, this.#depth * 2);
+        this.#reportDropped(frame, "staging-busy");
+    }
+    /**
+     * @internal shaderloom patch (T1295): Frame.attachExternalSpan's undo. Work that attached a span
+     * and then never reached the queue — it threw, or its command buffer failed to finish or submit —
+     * wrote no timestamps, so the frame must not resolve its pair into a result: that would be a
+     * duration for a pass that never ran. The pair stays reserved (indices are positional) and is
+     * simply never reported.
+     */
+    retractSpan(span, frame) {
+        if (frame !== this.#frame)
+            return;
+        const index = this.#frameSpans.findIndex((entry) => entry.name === span.name);
+        if (index >= 0)
+            this.#frameSpans.splice(index, 1);
     }
     /** @internal Frame.submit hook, after queue.submit succeeds: starts the non-blocking readback. */
     frameSubmitted(frame) {
@@ -181,7 +232,7 @@ export class InternalTimer {
             if (seq <= this.#appliedSeq)
                 return;
             this.#appliedSeq = seq;
-            this.#dispatch(spans, values);
+            this.#dispatch(spans, values, frame);
         });
     }
     /**
@@ -191,16 +242,24 @@ export class InternalTimer {
      * it attached a span — the counterpart of frameSubmitted() for frames that never submit.
      */
     frameAbandoned(frame) {
-        if (frame === this.#frame)
+        if (frame === this.#frame) {
             this.#encodedSpans = undefined;
+            // shaderloom patch (T1295): a failed pass discards this timer for the WHOLE frame, and a
+            // failed finish/submit or a cancel() never reaches the queue. Either way the frame's spans
+            // produce no result; they used to vanish, and now the frame is reported once.
+            if (!this.#disposed && this.#frameSpans.length > 0)
+                this.#reportDropped(frame, "abandoned");
+        }
         this.#releaseRing(frame);
     }
     #beginFrame(frame) {
-        if (this.#demand > this.#ring.capacity) {
+        // shaderloom patch (T1295): the staging DEPTH grows on the same edge as the capacity.
+        if (this.#demand > this.#ring.capacity || this.#depthDemand > this.#depth) {
             // Grow only at frame boundaries. The retired ring keeps applying its in-flight readbacks
             // before releasing its resources, so growth never drops results already submitted.
             this.#ring.dispose();
             this.#capacity = Math.min(MAX_QUERIES, nextPowerOfTwo(this.#demand));
+            this.#depth = this.#depthDemand;
             this.#ring = this.#createRing();
         }
         this.#demand = this.#capacity;
@@ -209,19 +268,51 @@ export class InternalTimer {
         this.#frameNames.clear();
         this.#usedQueries = 0;
     }
-    #dispatch(spans, values) {
+    #dispatch(spans, values, frame) {
         const results = {};
+        // shaderloom patch (T1243): the FRAME's extent — earliest begin to latest end across every
+        // span of this frame — handed to listeners beside the per-span durations. Per-pass spans
+        // cannot be summed into a frame figure: on Apple GPUs (Dawn/Metal, stage-boundary
+        // sampling) every pass's begin timestamp lands near the start of the command buffer while
+        // the ends are sequential, so the spans nest and their sum is ~N/2 times the frame. The
+        // raw timestamps that resolve this are only ever seen here, and were discarded.
+        let first = null;
+        let last = null;
         for (const { name, begin } of spans) {
             const start = values[begin] ?? 0n;
             const end = values[begin + 1] ?? 0n;
             // Timestamps are implementation-defined ns ticks and the counter may reset between writes
             // ("unexpected values such as negative deltas ... can safely be discarded"): clamp negatives to 0.
             results[name] = end > start ? Number(end - start) * MS_PER_NS : 0;
+            if (end > start) {
+                if (first === null || start < first)
+                    first = start;
+                if (last === null || end > last)
+                    last = end;
+            }
         }
         const frozen = Object.freeze(results);
+        const extent = Object.freeze({
+            extentMs: first !== null && last !== null ? Number(last - first) * MS_PER_NS : 0,
+            frame,
+        });
         for (const listener of [...this.#listeners]) {
             try {
-                listener(frozen);
+                listener(frozen, extent);
+            }
+            catch (error) {
+                console.error(error);
+            }
+        }
+    }
+    #reportDropped(frame, reason) {
+        if (this.#droppedFrame === frame)
+            return;
+        this.#droppedFrame = frame;
+        const drop = Object.freeze({ frame, reason, spans: this.#frameSpans.length });
+        for (const listener of [...this.#dropListeners]) {
+            try {
+                listener(drop);
             }
             catch (error) {
                 console.error(error);
@@ -242,7 +333,7 @@ export class InternalTimer {
         ring.release();
     }
     #createRing() {
-        return createQueryRing(this.#device, { type: "timestamp", capacity: this.#capacity, label: "vgpu.timer", trackSettled: this.#host.trackSettled, errorSink: this.#host.errorSink });
+        return createQueryRing(this.#device, { type: "timestamp", capacity: this.#capacity, depth: this.#depth, label: "vgpu.timer", trackSettled: this.#host.trackSettled, errorSink: this.#host.errorSink });
     }
     #assertUsable(where) {
         if (this.#disposed)
