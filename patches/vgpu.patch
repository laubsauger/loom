diff --git a/dist/target-offscreen.js b/dist/target-offscreen.js
index 3b4221148054e92c95a08f089a637249297b8d51..3757a969c867303556854e9998db66c917b98ec7 100644
--- a/dist/target-offscreen.js
+++ b/dist/target-offscreen.js
@@ -87,6 +87,8 @@ export class OffscreenTarget {
             size: this.#currentSize,
             format: spec.format,
             usage: ["render_attachment", "texture_binding", "copy_src"],
+            // shaderloom patch (T1307): same allocation, alternate sampling interpretation.
+            viewFormats: this.options.viewFormats,
             sampleCount: 1,
             label: this.options.label ? `${this.options.label}.color${index}.resolve` : undefined,
         }));
diff --git a/dist/target.d.ts b/dist/target.d.ts
index f642ba8b4a9ba90b3d3aff09484779766564f86a..4b158b1c64066e33c1e36cd4bcc055a515d7b71b 100644
--- a/dist/target.d.ts
+++ b/dist/target.d.ts
@@ -2,6 +2,8 @@ import type { ClearColor } from "./target-utils.ts";
 import type { Texture, ResourceDestroyCallback, ResourceIdentity, UnsubscribeResourceDestroy } from "@vgpu/core";
 export interface TargetTextureOptions {
     readonly format?: GPUTextureFormat;
+    /** shaderloom T1307: compatible views for each resolved color attachment; not depth/MSAA storage. */
+    readonly viewFormats?: readonly GPUTextureFormat[];
     /**
      * Default clear color of this target, used by passes that clear without naming a color.
      * Defaults to `[0, 0, 0, 1]`; mutable at runtime through `target.clearColor`.
diff --git a/dist/compute.js b/dist/compute.js
index fa1d930d97886ed6168d1ce9598f810e9ce61e15..076a88d671527de0c7da84dc1e1a4ca6e78d7c3e 100644
--- a/dist/compute.js
+++ b/dist/compute.js
@@ -71,6 +71,16 @@ export class ComputePipeline {
         if (opts.set)
             this.set(opts.set);
     }
+    /**
+     * shaderloom patch (T1180): drop this compute's cached bind groups; see
+     * InternalDraw.evictBindGroups. A compute shares the gpu's one bind cache with every draw
+     * while `nextComputeId` is a counter separate from `nextDrawId`, so one id can name both —
+     * the worst case is still a rebuilt bind group on the other one's next encode, never a
+     * wrong one. Its own pipeline and shader module are per-instance and are left to the GC.
+     */
+    evictBindGroups() {
+        this.cache.clearDraw(this.id);
+    }
     set(values) {
         assertDeviceUsable(this.device, `${this.label}.set`);
         this.setCore.set(values);
diff --git a/dist/draw.js b/dist/draw.js
index d6011f8aba886f4a9f5f4e15b7da07fe889ad006..e4036ed78a5ca3a46590a3b8f121d2e926a5d2fa 100644
--- a/dist/draw.js
+++ b/dist/draw.js
@@ -102,6 +102,35 @@ export class InternalDraw {
     writesDepth() { return drawWritesDepth(this); }
     /** @internal Frame drawable protocol; see {@link drawStencilWritingOps}. */
     stencilWritingOps() { return drawStencilWritingOps(this); }
+    /**
+     * shaderloom patch (T1180): drop this draw's cached bind groups. It calls the cache's own
+     * `clearDraw`, which upstream defines and never calls; nothing else can reach it, because
+     * the cache is a private kernel service and `drawId` is a closure-local counter.
+     *
+     * Two ways entries become unreachable, and neither has a public exit today:
+     *
+     * 1. A DISCARDED drawable. The key is `drawId:group:identities` and every Draw/Effect
+     *    mints a fresh `nextDrawId`, so a program rebuild's replaced Effects leave their
+     *    entries behind for the life of the device — Draw/Effect/Compute expose no destroy at
+     *    all. `subscribeEviction` reclaims an entry only when a resource it names is
+     *    destroyed, so entries over surviving resources (a shader edit rebinding the same
+     *    target) survive with it.
+     *
+     * 2. A LIVE drawable whose bindings ROTATE. The eviction subscription is per binding
+     *    SLOT, not per cache entry: `set()`ing a slot to a new resource unsubscribes the
+     *    previous one, so an entry naming the old resource outlives that resource's destroy
+     *    with nothing listening. A blit that re-points one texture slot at each of N targets
+     *    keeps N entries and is subscribed to one.
+     *
+     * Only this draw's own entries go — the drawId prefix is unique per instance — and any
+     * entry still wanted is rebuilt on the next encode (`setCore.bindGroups()` calls
+     * `getOrCreate` every time), so an over-eager evict costs a rebuild, never a wrong bind
+     * group. Pipelines, shader modules and layouts are shared by key and are untouched.
+     */
+    evictBindGroups() {
+        const state = drawState(this);
+        state.cache.clearDraw(state.id);
+    }
     set(values) {
         const state = drawState(this);
         assertDeviceUsable(state.device, `${this.label}.set`);
diff --git a/dist/effect.js b/dist/effect.js
index 079c6686ce2c87a580f217b12c9ea978f0ed3005..fb684c09ba66ed7763f8f2123a5f6ec635bda0ea 100644
--- a/dist/effect.js
+++ b/dist/effect.js
@@ -31,6 +31,8 @@ export class InternalEffect {
         effectImpls.set(this, impl);
     }
     set(values) { effectImpl(this).set(values); return this; }
+    /** shaderloom patch (T1180): forwards to the underlying draw; see InternalDraw.evictBindGroups. */
+    evictBindGroups() { effectImpl(this).evictBindGroups(); }
     draw(target = {}) { effectImpl(this).draw(isTarget(target) ? { target } : target); }
     compile(target) { return effectImpl(this).compile(target).then(() => this); }
     compileSync(target) { effectImpl(this).compileSync(target); return this; }
diff --git a/dist/frame.js b/dist/frame.js
index 21e3d41add2578fee9198c44260cba69b7867c82..3b4ac37fdfa159b02d792a8378fefa55f3a1a4bd 100644
--- a/dist/frame.js
+++ b/dist/frame.js
@@ -103,8 +103,8 @@ export class Frame {
             throw surfaceNotInFrameError("Frame.pass");
         const clear = targetOnly ? undefined : target.clear;
         const preserve = clear === false;
-        if (preserve && resolvedTarget.sampleCount === 4)
-            throw passPreserveMsaaError();
+        // shaderloom patch (T939): preserve into MSAA is supported now — the msaa
+        // attachment stores every pass and loads on preserve (see colorAttachment).
         const clearDepth = targetOnly ? undefined : target.clearDepth;
         if (clearDepth !== undefined) {
             if (typeof clearDepth !== "number" || !(clearDepth >= 0 && clearDepth <= 1))
diff --git a/dist/index.d.ts b/dist/index.d.ts
index e37316bd0fe8c02e3346594169c95c394531e089..d2d15e71d436c11cd492c21748f164c9f95e10f0 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,6 +1,6 @@
 import { createGpu, type InitOptions } from "./init.ts";
 export { initFromDevice } from "./init-from-device.ts";
-export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
+export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerFrameExtent, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
 export type { BlendComponentOptions, BlendOptions, BlendPreset, DepthOptions, Draw, DrawOptions, DrawCallOptions, DrawLayoutOptions, GeometryLike, StencilFaceOptions, StencilOptions } from "./draw.ts";
 export { Geometry } from "./scene/geometry-descriptor.ts";
 export type { GeometryAttributeOverride, GeometryAttributes, GeometryBuffer, GeometryBufferOptions, GeometryData, GeometryOptions, GeometrySlice, GeometrySliceOptions } from "./scene/geometry-descriptor.ts";
diff --git a/dist/init.d.ts b/dist/init.d.ts
index d4cb959be6bc7d6e9c8fbad4d42cd90058c1ade1..2a25b0e6e314510025de4fe005d091069ac0a838 100644
--- a/dist/init.d.ts
+++ b/dist/init.d.ts
@@ -3,7 +3,7 @@ export { initFromDevice } from "./init-from-device.ts";
 export type { AdapterFactory, InitOptions } from "./kernel.ts";
 export type { Compute, ComputeOptions, DispatchOptions, GpuErrorListener, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions } from "./api-types.ts";
 export type { ClearColor } from "./target-utils.ts";
-export type { Timer, TimerSpan } from "./timer.ts";
+export type { Timer, TimerFrameExtent, TimerSpan } from "./timer.ts";
 export type { Visibility, VisibilityOptions, VisibilityQuery } from "./visibility.ts";
 export type { Bundle, BundleOptions, BundleRecorder } from "./bundle.ts";
 export type { Surface, SurfaceOptions, SurfaceResizeEvent } from "./surface.ts";
diff --git a/dist/mock.d.ts b/dist/mock.d.ts
index 8fe9da7bf42e35f413c59ba076525e1302f055e0..a64870b5bf1868d9801a737315f340d1e8413a51 100644
--- a/dist/mock.d.ts
+++ b/dist/mock.d.ts
@@ -3,7 +3,7 @@ export { initFromDevice } from "./init-from-device.ts";
 export { createMockAdapter } from "@vgpu/adapter-mock";
 export type { CreateMockAdapterOptions } from "@vgpu/adapter-mock";
 export { getMockGPUDeviceInstrumentation } from "@vgpu/core";
-export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
+export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, InitOptions, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerFrameExtent, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
 export type { BlendComponentOptions, BlendOptions, BlendPreset, DepthOptions, Draw, DrawOptions, DrawCallOptions, DrawLayoutOptions, GeometryLike, StencilFaceOptions, StencilOptions } from "./draw.ts";
 export { Geometry } from "./scene/geometry-descriptor.ts";
 export type { GeometryAttributeOverride, GeometryAttributes, GeometryBuffer, GeometryBufferOptions, GeometryData, GeometryOptions, GeometrySlice, GeometrySliceOptions } from "./scene/geometry-descriptor.ts";
diff --git a/dist/node.d.ts b/dist/node.d.ts
index 0b778eaed8b166bbf1e2b0eb5f7dfd5292c1db87..b5b80d0851992e39fc20fedfc9b6d730610ef1b4 100644
--- a/dist/node.d.ts
+++ b/dist/node.d.ts
@@ -2,7 +2,7 @@ import { type NodeAdapterInfo, type NodeAdapterMode } from "@vgpu/adapter-node";
 import type { VGPUAdapter } from "@vgpu/core";
 import { type Gpu, type InitOptions } from "./init.ts";
 export { createNodeAdapter } from "@vgpu/adapter-node";
-export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
+export type { Bundle, BundleOptions, BundleRecorder, Compute, ComputeOptions, DispatchOptions, ClearColor, GpuErrorListener, PingPongStorage, PingPongTargets, SharedUniforms, StorageAccess, StorageBuffer, StorageOptions, Surface, SurfaceOptions, SurfaceResizeEvent, Timer, TimerFrameExtent, TimerSpan, Visibility, VisibilityOptions, VisibilityQuery } from "./init.ts";
 export type { BlendComponentOptions, BlendOptions, BlendPreset, DepthOptions, Draw, DrawOptions, DrawCallOptions, DrawLayoutOptions, GeometryLike, StencilFaceOptions, StencilOptions } from "./draw.ts";
 export { Geometry } from "./scene/geometry-descriptor.ts";
 export type { GeometryAttributeOverride, GeometryAttributes, GeometryBuffer, GeometryBufferOptions, GeometryData, GeometryOptions, GeometrySlice, GeometrySliceOptions } from "./scene/geometry-descriptor.ts";
diff --git a/dist/set-resources.js b/dist/set-resources.js
index 43d3530106957dcbd764af7bd07f91b8d8b14ef3..ccc4d62b4cd3708633a7a0473b3d9c8d89a622cd 100644
--- a/dist/set-resources.js
+++ b/dist/set-resources.js
@@ -47,8 +47,15 @@ function normalizeBufferResource(binding, value, context) {
         assertBufferUsable(value.buffer, `${context.sourceHint}.set`);
         return { resource: { buffer: value.gpu, offset: 0, size: value.size }, identity: value.buffer.resourceIdentity, unsubscribe: (cb) => value.buffer.onDestroy(cb) };
     }
-    if (isGPUBufferBinding(value))
-        return { resource: value, identity: syntheticIdentity(value.buffer) };
+    if (isGPUBufferBinding(value)) {
+        // shaderloom patch (T1076): a REGION binding may name its own identity. Without
+        // this, every region of one buffer shares that buffer's synthetic identity, so the
+        // bind-group cache cannot tell two attributes apart and #preflightAliasing reports
+        // two NON-OVERLAPPING writable regions as an alias. Point storage is packed —
+        // several attributes per buffer — and both consequences are wrong.
+        const region = { buffer: value.buffer, offset: value.offset, size: value.size };
+        return { resource: region, identity: value.resourceIdentity ?? syntheticIdentity(value.buffer) };
+    }
     if (isRawGPUBuffer(value))
         return { resource: { buffer: value }, identity: syntheticIdentity(value) };
     throw incompatibleResourceError(binding, "buffer", `Pass a compatible Buffer/Uniform: ${binding.name}.set({ ${binding.name}: gpu.device.createBuffer(...) }).`);
@@ -113,7 +120,13 @@ function asTarget(value) {
 }
 function hasAnyResourceShape(value) {
     const record = value;
-    return "gpu" in record || "bindGroup" in record || "createView" in record || "resourceIdentity" in record;
+    // shaderloom patch (T1076): a raw GPUBufferBinding ({ buffer, offset, size }) is a
+    // resource, not a JS uniform value. Without this it falls to isPlainValue() and the
+    // set() routes it at the uniform path, throwing VGPU-RING1-UNSUPPORTED — even though
+    // normalizeBufferResource() below already handles the shape (isGPUBufferBinding).
+    // Binding one region of a packed buffer is how point attributes reach shaders that
+    // still declare array<vec3f>.
+    return "gpu" in record || "bindGroup" in record || "createView" in record || "resourceIdentity" in record || isGPUBufferBinding(record);
 }
 function syntheticIdentity(value) {
     if (typeof value !== "object" || value === null)
diff --git a/dist/target-utils.js b/dist/target-utils.js
index ec39e15a23d7dfdf7172e74d7c7305b34670a3e6..f9f04834e0554f53331cdb50872d04f6ef5cf94c 100644
--- a/dist/target-utils.js
+++ b/dist/target-utils.js
@@ -53,11 +53,15 @@ function validateMsaaFormat(format, caps) {
     throw unsupportedError("target", "Dawn compatibility mode does not support rgba16float+msaa.", "Use rgba8unorm for MSAA here, or disable msaa.");
 }
 export function colorAttachment(resolved, msaa, clear, preserve) {
+    // shaderloom patch (T939): multi-pass MSAA. The multisampled texture persists on the
+    // target, so preserve is legal WebGPU: load it, store it, and resolve every pass so
+    // the resolved color stays current for mid-chain readers. Non-preserve passes keep
+    // the cheaper discard.
     const attachment = {
         view: (msaa ?? resolved).createView(),
         resolveTarget: msaa ? resolved.createView() : undefined,
         loadOp: preserve ? "load" : "clear",
-        storeOp: msaa ? "discard" : "store",
+        storeOp: "store",
     };
     if (!preserve)
         attachment.clearValue = colorValue(clear);
@@ -74,13 +78,15 @@ export function depthAttachment(depth, preserve, clearDepth, clearStencil, readO
             attachment.stencilReadOnly = true;
         return attachment;
     }
-    const attachment = { view: depth.createView(), depthLoadOp: preserve ? "load" : "clear", depthStoreOp: depth.sampleCount > 1 ? "discard" : "store" };
+    // shaderloom patch (T939): multisampled depth STORES too — a later preserve pass
+    // depth-tests against it, and a discarded depth would load garbage.
+    const attachment = { view: depth.createView(), depthLoadOp: preserve ? "load" : "clear", depthStoreOp: "store" };
     if (!preserve)
         attachment.depthClearValue = clearDepth ?? 1;
     // WebGPU requires stencilLoadOp/stencilStoreOp whenever the format has a stencil aspect and stencilReadOnly is not set.
     if (depth.format && hasStencilAspect(depth.format)) {
         attachment.stencilLoadOp = preserve ? "load" : "clear";
-        attachment.stencilStoreOp = depth.sampleCount > 1 ? "discard" : "store";
+        attachment.stencilStoreOp = "store";
         if (!preserve)
             attachment.stencilClearValue = clearStencil ?? 0;
     }
diff --git a/dist/timer.d.ts b/dist/timer.d.ts
index 4571da5a860793c7250ad8862c5341d700217765..d90c34b8f3238e77907b5045e46727582e84c633 100644
--- a/dist/timer.d.ts
+++ b/dist/timer.d.ts
@@ -10,12 +10,21 @@ import { type QueryHostOptions } from "./query-ring.ts";
 export interface TimerSpan {
     readonly name: string;
 }
+/**
+ * shaderloom patch (T1243): what one frame's spans say about the FRAME. `extentMs` is the earliest
+ * span begin to the latest span end of that frame's submit; `frame` is the identity of the vgpu
+ * frame the spans were attached to, so a caller can tie the result back to what it encoded.
+ */
+export interface TimerFrameExtent {
+    readonly extentMs: number;
+    readonly frame: object;
+}
 /** GPU pass timer created by `timer(gpu)`. Needs the "timestamp-query" device feature. */
 export interface Timer {
     /** Marks a pass for timing. Pass the result as FramePassOptions.timer. One name per frame. */
     span(name: string): TimerSpan;
-    /** Registers a callback invoked whenever a new set of results lands (typically 1-2 frames after submit). Times in milliseconds. Returns an unsubscribe function. */
-    onResults(cb: (spans: Readonly<Record<string, number>>) => void): () => void;
+    /** Registers a callback invoked whenever a new set of results lands (typically 1-2 frames after submit). Times in milliseconds. Returns an unsubscribe function. shaderloom patch (T1243): the frame's extent rides as a second argument. */
+    onResults(cb: (spans: Readonly<Record<string, number>>, frame: TimerFrameExtent) => void): () => void;
     /** Releases the timer's query set and buffers. Further use of the timer or its spans throws. */
     dispose(): void;
 }
@@ -54,7 +63,7 @@ export declare class InternalTimer {
     #private;
     constructor(device: Device, host?: QueryHostOptions);
     span(name: string): TimerSpan;
-    onResults(cb: (spans: Readonly<Record<string, number>>) => void): () => void;
+    onResults(cb: (spans: Readonly<Record<string, number>>, frame: TimerFrameExtent) => void): () => void;
     dispose(): void;
     /**
      * @internal Frame.pass hook: registers the span for the current frame and returns the pass
diff --git a/dist/timer.js b/dist/timer.js
index 88f211164c7f4d19a965d45ef4cce5d75fe08086..c151bece3125ee8d4e976a7632e909337c906ab0 100644
--- a/dist/timer.js
+++ b/dist/timer.js
@@ -181,7 +181,7 @@ export class InternalTimer {
             if (seq <= this.#appliedSeq)
                 return;
             this.#appliedSeq = seq;
-            this.#dispatch(spans, values);
+            this.#dispatch(spans, values, frame);
         });
     }
     /**
@@ -209,19 +209,37 @@ export class InternalTimer {
         this.#frameNames.clear();
         this.#usedQueries = 0;
     }
-    #dispatch(spans, values) {
+    #dispatch(spans, values, frame) {
         const results = {};
+        // shaderloom patch (T1243): the FRAME's extent — earliest begin to latest end across every
+        // span of this frame — handed to listeners beside the per-span durations. Per-pass spans
+        // cannot be summed into a frame figure: on Apple GPUs (Dawn/Metal, stage-boundary
+        // sampling) every pass's begin timestamp lands near the start of the command buffer while
+        // the ends are sequential, so the spans nest and their sum is ~N/2 times the frame. The
+        // raw timestamps that resolve this are only ever seen here, and were discarded.
+        let first = null;
+        let last = null;
         for (const { name, begin } of spans) {
             const start = values[begin] ?? 0n;
             const end = values[begin + 1] ?? 0n;
             // Timestamps are implementation-defined ns ticks and the counter may reset between writes
             // ("unexpected values such as negative deltas ... can safely be discarded"): clamp negatives to 0.
             results[name] = end > start ? Number(end - start) * MS_PER_NS : 0;
+            if (end > start) {
+                if (first === null || start < first)
+                    first = start;
+                if (last === null || end > last)
+                    last = end;
+            }
         }
         const frozen = Object.freeze(results);
+        const extent = Object.freeze({
+            extentMs: first !== null && last !== null ? Number(last - first) * MS_PER_NS : 0,
+            frame,
+        });
         for (const listener of [...this.#listeners]) {
             try {
-                listener(frozen);
+                listener(frozen, extent);
             }
             catch (error) {
                 console.error(error);
